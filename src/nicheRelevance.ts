const normalize=(value:string)=>value.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
const aliases: Record<string,string[]> = { academia:['academia','fitness','treinamento','musculação','crossfit','pilates','yoga'], barbearia:['barbearia','barber','corte masculino'], 'salão de beleza':['salão','beleza','cabeleireiro','cabelos'], restaurante:['restaurante','gastronomia','cozinha'], 'restaurante japonês':['japonês','japanese','sushi','temakeria','izakaya','ramen'], 'clínica de estética':['estética','beleza','dermatologia'], dentista:['odontologia','dentista','odonto'], advocacia:['advocacia','advogado','jurídico'] };
const geoapify: Record<string,string[]> = { academia:['commercial.sports_centre','sport.fitness'], barbearia:['service.beauty.hairdresser'], 'salão de beleza':['service.beauty.hairdresser'], restaurante:['catering.restaurant'], 'restaurante japonês':['catering.restaurant.japanese','catering.restaurant.sushi','catering.restaurant.ramen'], 'clínica de estética':['healthcare','service.beauty'], dentista:['healthcare.dentist'], advocacia:['office.lawyer'] };
const matchingKey = (table: Record<string, unknown>, query: string) => Object.keys(table).sort((a,b)=>b.length-a.length).find(item=>query.includes(normalize(item)));
export function geoapifyCategories(niche:string) { const query=normalize(niche); const key=matchingKey(geoapify, query); return geoapify[key || 'restaurante']; }
export function isNicheRelevant(name:string,category:string|undefined,niche:string){const query=normalize(niche);const key=matchingKey(aliases, query);const terms=key?aliases[key]:[query];const haystack=normalize(`${name} ${category||''}`);return terms.some(term=>haystack.includes(normalize(term)));}
