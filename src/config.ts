import { z } from 'zod';
const boolEnv = z.preprocess((value) => typeof value === 'string' ? value.toLowerCase() === 'true' : value, z.boolean());
const schema=z.object({NODE_ENV:z.enum(['development','test','production']).default('development'),PORT:z.coerce.number().default(3000),DATABASE_URL:z.string().optional(),RADAR_DATABASE_URL:z.string().optional(),RADAR_DATABASE_DATABASE_URL:z.string().optional(),REDIS_URL:z.string().optional(),PROVIDER_MODE:z.string().default('meta-cloud-api'),WHATSAPP_PROVIDER:z.string().optional(),TEST_MODE:boolEnv.default(true),DISCOVERY_MODE:z.enum(['real','simulation']).default('simulation'),DISCOVERY_TEST_MODE:boolEnv.default(true),GEOAPIFY_API_KEY:z.string().optional(),GOOGLE_PLACES_API_KEY:z.string().optional(),SEARCH_PROVIDER_API_KEY:z.string().optional(),TAVILY_API_KEY:z.string().optional(),CNPJWS_ENRICHMENT_ENABLED:boolEnv.default(false),OSM_CONTACT_ENRICHMENT_ENABLED:boolEnv.default(false),META_ACCESS_TOKEN:z.string().optional(),META_PHONE_NUMBER_ID:z.string().optional(),META_WABA_ID:z.string().optional(),META_APP_SECRET:z.string().optional(),META_WEBHOOK_VERIFY_TOKEN:z.string().default('change-me'),META_GRAPH_API_VERSION:z.string().default('v23.0'),JWT_SECRET:z.string().default('local-only-change-me'),ADMIN_EMAIL:z.string().default('admin@impulsesites.local'),ADMIN_PASSWORD:z.string().optional(),PUBLIC_LEAD_CAPTCHA_SECRET:z.string().optional(),ALLOWED_ORIGINS:z.string().default('http://localhost:5173')});
export const env=schema.parse(process.env); export const databaseUrl=env.RADAR_DATABASE_DATABASE_URL||env.RADAR_DATABASE_URL||env.DATABASE_URL; export function configuredProvider(){return env.WHATSAPP_PROVIDER||env.PROVIDER_MODE;} export function discoveryRealMode(){return env.DISCOVERY_MODE==='real'||!env.DISCOVERY_TEST_MODE;} export function assertOfficialProvider(){if(env.NODE_ENV==='production'&&env.TEST_MODE)throw new Error('Production cannot run with TEST_MODE=true.');if(env.NODE_ENV==='production'&&configuredProvider()!=='meta-cloud-api')throw new Error('Production requires the official Meta WhatsApp Cloud API provider.');if(discoveryRealMode()&&env.DISCOVERY_TEST_MODE)throw new Error('DISCOVERY_MODE=real requires DISCOVERY_TEST_MODE=false.');if(env.NODE_ENV==='production'&&!env.ADMIN_PASSWORD)throw new Error('Production requires ADMIN_PASSWORD.');if(env.NODE_ENV==='production'&&env.JWT_SECRET==='local-only-change-me')throw new Error('Production requires a strong JWT_SECRET.');}
