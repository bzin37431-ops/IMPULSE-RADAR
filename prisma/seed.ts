import { PrismaClient, LeadStatus, Role, ConsentStatus } from '@prisma/client';
const prisma=new PrismaClient();
async function main(){const admin=await prisma.user.upsert({where:{email:'admin@impulsesites.local'},update:{},create:{email:'admin@impulsesites.local',name:'Admin Impulse',role:Role.ADMIN}});const leads=[{name:'Marina Costa',company:'Ateliê Forma',phone:'+5511987654321',normalizedPhone:'+5511987654321',city:'São Paulo',state:'SP',niche:'Clínica estética',status:LeadStatus.INTERESSADO,source:'SITE'},{name:'Rafael Lima',company:'Studio Norte',phone:'+5511976543210',normalizedPhone:'+5511976543210',city:'Campinas',state:'SP',niche:'Arquitetura',status:LeadStatus.CONTATADO,source:'FORMULARIO'},{name:'Ana Martins',company:'Casa Essencial',phone:'+5521987654321',normalizedPhone:'+5521987654321',city:'Rio de Janeiro',state:'RJ',niche:'Varejo',status:LeadStatus.NOVO,source:'MANUAL_DOCUMENTADO'}];for(const lead of leads){const saved=await prisma.lead.upsert({where:{normalizedPhone:lead.normalizedPhone},update:{},create:lead});await prisma.consent.upsert({where:{leadId:saved.id},update:{},create:{leadId:saved.id,status:ConsentStatus.OPTED_IN,source:lead.source}})}await prisma.template.upsert({where:{name:'impulse_apresentacao'},update:{},create:{name:'impulse_apresentacao',language:'pt_BR',category:'MARKETING',status:'APPROVED'}});console.log(`Seed concluído: ${admin.email}`)}
main().catch(e=>{console.error('Seed failed',e);process.exitCode=1}).finally(()=>prisma.$disconnect());
